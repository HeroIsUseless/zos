memory.z: @\kernel\memory.z;

[画一个像素]
pixel(x:dword, 
      y:dword, 
      color:dword, 
      screenWidth:dword, 
      vramAddr:dword):(
  addr: x + y * screenWidth + vramAddr;
  memory.z\set.1byte(color, addr);
);