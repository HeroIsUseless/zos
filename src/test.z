draw.z: @\screen\draw.z;

draw(addrVram: dword):(
  i: 0;
  while(i < 300, (draw.z\pixel(i, i, i, 320, addrVram);i<=i+1;));
  x: 0;
  y: 0;
  while(y\<200, (
    x <= 0;
    while(x\<320, (
      draw.z\pixel(x, y, x, 320, addrVram);
      x <= x+1;
    ));
    y <= y+1;
  ));
);
